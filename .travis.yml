language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t shunsukenagashima/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shunsukenagashima/docker-react npm run test